# include <stdio.h>
int main() {
    int Y;
    scanf("%d",&Y);
    if ((Y%4==0 && Y%100!=0)||(Y%400==0)) {
        printf("%d is leap year",Y);
    }
    else {
        printf("%d is not a leap year",Y);
    }
    return 0;
}