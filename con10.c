#include<stdio.h>
int main(){
    int num;
    scanf("%d", &num);
if(num>100 && num<999){
    printf("Three digit number",num);
}    
else{
    printf("Three digit number",num);
}
return 0;
}